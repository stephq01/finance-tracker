// Removed — was causing more trouble than it was worth on phone screens.
// Kept as a no-op (rather than deleting the file and every import of it)
// so nothing else needs to change.
export default function PageGlow() {
  return null
}
